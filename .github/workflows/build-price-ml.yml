name: Build ML

on:
  push:
    branches:
      - main
    paths:
      - "price-ml/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v3

      - name: Log in
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: |
          IMAGE="ghcr.io/${{ github.repository_owner }}/hardhat-ml:${{ github.sha }}"
          docker build -t $IMAGE -f price-ml/Containerfile ./price-ml
          docker push $IMAGE

